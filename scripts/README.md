### Scripts

This directory contains helper scripts and automation for the repository.

- `scaffold_service.py` – scaffolds a new FastAPI-based service following the platform architecture.

Run scripts from the repository root, for example:

```bash
python scripts/scaffold_service.py user_service
```

